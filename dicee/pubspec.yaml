name: dicee
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: "^3.3.4"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - images/
