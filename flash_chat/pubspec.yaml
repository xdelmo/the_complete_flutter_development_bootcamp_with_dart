name: flash_chat
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: "3.4.3"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^8.0.0
  http: ^1.2.1
  animated_text_kit: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - images/
